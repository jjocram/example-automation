from django.test import TestCase

class ExampleTest(TestCase):
    def test_to_test_tests_are_working(self):
        """
        Assert always true, simple test to test that tests are working correctly
        """
        self.assertIs(True, True)
